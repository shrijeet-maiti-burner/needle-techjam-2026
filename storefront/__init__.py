"""Presentation layer for the conversational storefront.

Nothing here is imported by ``needle``, ``starter`` or ``submission``, and this
package is not in ``SHIPPING_PATHS``, so it cannot reach the scored bundle. It
reads the catalog and the agent's public response; it never influences either.
"""
