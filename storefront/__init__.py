"""Presentation layer for the conversational storefront.

Nothing here is imported by ``needle``, ``starter`` or ``submission``. The
package ships as an optional judge-facing demonstration, but it cannot reach
the official scoring entry point. It reads the catalog and the agent's public
response; it never influences either.
"""
