# EXP-013 question policy evidence

Status: registered question answered, no code change required.

## Registered question

"Does answerability-aware questioning beat fixed, entropy-only, repeated-`other`,
and no-question controls, at identical safe slates?"

Answer: no. Repeated `other` beats every alternative arm measured. The shipped
line in `needle/agent.py` (`ask_attribute = "other" if turn < 10 else None`) is
retained on evidence rather than on convenience.

Falsification threshold registered before the run: an alternative policy must
gain at least +0.005 TechnicalScore over repeated `other` without losing HR@10 on
any scenario slice. No arm cleared it, and no arm beat repeated `other` at all.
The closest alternative question policy lost 0.003722; the closest arm of any
kind lost 0.003264 and also changed state admission rather than questioning.

The arms were first run at the pre-#11 primary and rerun at the current one
after #11 changed retrieval. Both runs are reported. The ordering is unchanged
and the conclusion is unchanged, but several magnitudes moved enough to matter,
and the rerun is the one that describes the shipped agent.

## Pins

- code, first run: local commit `c5930ff`, clean tree. That commit reached
  `main` squashed through PR #5 and is not an ancestor of it, so it cannot be
  checked out from the remote; the rerun below supersedes it as the citable
  measurement.
- code, rerun: this branch, clean tree
- evaluator: official `local_evaluator.py`, source commit
  `34078351e1c3615e5505a2e829600b56a542e462`
- public set: sha256 `857259f7a438e6188ac63e18995b6ff4489bfcfc4a716a798b9a2aa0ee8f7579`
- catalog: sha256 `da979b05a68af864cb0dcf9ee6a81c010c7e66a57978ad286c7a2e005fc69a67`
- Python 3.12.3, macOS, stdlib only, no network, no model assets
- fixed controls across all arms: whatever `PRIMARY_AGENT_KWARGS` holds, which
  `scripts/qpolicy_arms.py` now reads rather than copies. The first run was at
  `popularity_strength=0.20` with no category prior; the rerun is at the current
  primary, `category_strength=1.00` and `popularity_strength=0.30`. Only
  `ask_attribute` selection varies between arms, except arms B and G which also
  vary state admission (noted inline).
- arms were applied by monkeypatching `Agent.respond` after the shipped call, so
  slates are byte-identical across arms at equal message histories
- all 200 official public sessions per arm, zero contract violations
- both headline arms were rerun and reproduced to the printed digit

## Results

| Arm | Question policy | TechnicalScore | HR@10 | MRR | MTTC | Override HR | Boundary HR | pre-#11 score |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| A | repeated `other` (shipped) | **0.878039** | 0.995 | 0.684131 | 2.235 | 1.000 | 1.000 | 0.868395 |
| B | repeated `other`, uninformative replies withheld from the query | 0.874775 | 0.995 | 0.671250 | 2.205 | 1.000 | 1.000 | 0.864818 |
| F | `other` x2, then bucket rotation | 0.874317 | 0.995 | 0.674056 | 2.270 | 1.000 | 1.000 | 0.859244 |
| D | `other` x2, then stop asking | 0.860875 | 0.980 | 0.660917 | 2.370 | 0.933 | 0.900 | 0.839695 |
| J | `feature`, `material`, then `other` | 0.854208 | 0.995 | 0.612692 | 2.355 | 1.000 | 1.000 | 0.857913 |
| H | answerability-ordered (`feature`/`material` alternating) | 0.851920 | 0.995 | 0.609067 | 2.415 | 1.000 | 1.000 | 0.848725 |
| I | frequency-ordered rotation | 0.845679 | 0.995 | 0.591262 | 2.460 | 1.000 | 1.000 | 0.849624 |
| G | bucket rotation, uninformative replies withheld | 0.824524 | 0.990 | 0.574415 | 3.140 | 0.967 | 1.000 | 0.800902 |
| E | fixed rotation over all 7 answerable buckets | 0.818835 | 0.995 | 0.552450 | 3.220 | 1.000 | 1.000 | 0.779538 |
| C | never ask (no-question control) | 0.565720 | 0.720 | 0.304734 | 5.285 | 0.667 | 0.500 | 0.443918 |

Every arm improved under #11 except J and I, and the ordering is otherwise
preserved. Arm A remains first and no arm beats it.

## What the arms establish

**Questioning is the largest single lever measured in this project so far.**
Arm C removes only `ask_attribute` and costs 0.312319 TechnicalScore, 27.5
points of HR@10, and 3.05 turns of MTTC. Retrieval quality is unchanged in that
arm; the loss is entirely the information the customer never discloses. Any
future change that suppresses questions, including a latency or "conversational
polish" change, must be measured against this number.

The cost fell from 0.424477 under the pre-#11 primary. Better retrieval recovers
part of what silence throws away, which is the expected direction: the category
prior gives the opening message more to rank on when no further constraint ever
arrives. Questioning is still the largest single lever measured in this project,
by a wide margin, but the margin is retrieval-dependent and should be requoted
whenever retrieval changes rather than carried forward.

**`other` dominates by construction, and the public numbers agree.** In
`customer_reply`, `other` matches any constraint type while a named attribute
matches only its own bucket, and both return at most two undisclosed values. A
named attribute can therefore return nothing where `other` would have returned
two; it can never return more. Every rotation arm (E, F, G, H, I, J) loses.

**Answerability ordering does not rescue rotation (H, I, J).** Across the 200
public sessions all 800 constraints classify as feature 404, material 302, color
60, style 19, size 11, use_case 4. Five of the seven answerable buckets are
nearly always empty, so a rotation spends most turns on a bucket that cannot
answer. Ordering by observed frequency (I) does not recover the loss and
trails `other` by 0.032360, because even `feature` misses whenever the remaining
undisclosed constraint is a material.

**Stopping early is a trap (D).** Every public session carries exactly four
constraints and `other` returns two per reply, so two questions look sufficient.
They are not: on boundary sessions the first question is consumed by the
no-preference reply, and on intent_override sessions the reply to the turn-2
question is discarded because the override message replaces it. Both slices need
a third question. That is visible as override HR 1.000 to 0.933 and boundary HR
1.000 to 0.900 in arm D. The agent cannot detect either case in advance, and
asking is free because `evaluate()` scores the slate before it reads
`ask_attribute`. So the correct policy is to ask on every turn that has a reply.

**Withholding uninformative replies from the retrieval query does not help
(B, G).** The prediction under test was that "I don't have an additional
preference for other." dilutes the BM25 query once constraints are exhausted.
It does not: arm B loses 0.003264 at identical HR@10, so it costs rank rather
than targets. The tokens are common enough
across the catalog that BM25 already discounts them, and the current
`NO_PREFERENCE_RE` handling in `needle/state.py` is sufficient because it
already stops those clauses from becoming negative constraints. This is a
recorded negative finding; the change was not made.

## Transfer risk to private

The rotation arms lose because of the bucket skew produced by
`classify_constraint`. Private targets are disjoint but the same classifier and
the same `intent_card` construction generate them, so the skew is expected to
hold; the conclusion should not be treated as verified there. `other` needs no
such assumption: it matches any type, so it is the arm that is robust to a skew
shift as well as the arm that wins on public. That is the reason to keep it,
independent of the public margin.

Public HR@10 for arm A is 0.995, so the question policy is saturated on public
and no further tuning is warranted against this set. The belief-state half of
EXP-013 landed separately in PRs #6, #9, and #10, so this closes the experiment.

## Decision and rollback

Keep `ask_attribute = "other" if turn < 10 else None`. No code change.
`tests/test_question_policy.py` pins the behavior and records why, so the line is
not silently replaced by an attribute rotation that costs 0.004 to 0.059.
Rollback is not applicable: nothing shipped.

Independent rerun owner: Shrijeet or Yazhiniyan, via
`python3 scripts/qpolicy_arms.py`.
